{% extends '_layout/base.html.twig' %}

{% block before_content %}
    <div class="navbar-expand-md">
        <div class="collapse navbar-collapse" id="navbar-menu">
            <div class="navbar navbar-light">
                <div class="container-xl">
                    <ul class="navbar-nav">
                        <li class="nav-item {% if app.request.get('_route') == 'project_overview' %}active{% endif %}">
                            <a class="nav-link" href="{{ path('project_overview', { slug: project.slug }) }}">
                                <span class="nav-link-title">
                                    Overview
                                </span>
                            </a>
                        </li>
                        <li class="nav-item {% if app.request.get('_route') == 'project_issues' %}active{% endif %}">
                            <a class="nav-link" href="{{ path('project_issues', { slug: project.slug }) }}">
                                <span class="nav-link-title">
                                    Issues
                                </span>
                            </a>
                        </li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
{% endblock %}